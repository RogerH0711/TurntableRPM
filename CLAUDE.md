# TurntableRPM — 給 Claude Code 的專案脈絡

用 iPhone 量測黑膠唱盤的轉速與抖晃率（wow & flutter）。目標精度 0.1%。
起因是一台 Thorens TD 235 EV 轉速偏慢，需要一個能量到 0.1% 的工具來診斷。

**回應語言：繁體中文（台灣）。程式碼註解也用中文。**

## 專案結構

```
TurntableRPM/
├── project.yml               XcodeGen 設定；.xcodeproj 不進版控
├── Config/Local.xcconfig     個人簽章設定（DEVELOPMENT_TEAM），不進版控
├── App/                      iOS app —— 只有這層碰 CoreMotion / SwiftUI / SwiftData
│   ├── MotionEngine.swift        CMMotionManager 封裝
│   ├── SampleAccumulator.swift   感測器佇列 <-> 主執行緒的交棒（NSLock）
│   └── LiveMeasurementView.swift 畫面
├── Packages/TurntableCore/   演算法核心
│   ├── Sources/                  純 Swift + Foundation，不 import UIKit/CoreMotion
│   ├── Tests/                    97 個測試
│   └── Reference/                Python 參考實作，黃金值的來源
├── tools/
│   └── analyze_export.py     分析 App 匯出的量測 JSON（含分段圓擬合）
└── docs/
    ├── spec.md               技術規格書（設計的權威來源）
    └── device-setup.md       上機步驟
```

## 指令

| 指令 | 用途 |
|---|---|
| `make test` | 演算法測試（97 個，約 4 秒，不需要模擬器或實機） |
| `make docker-test` | 同一批測試在 Linux container 跑一次（`swift:5.10`） |
| `make generate` | 新增/刪除 `App/` 底下的檔案後**必須**跑，重新產生 .xcodeproj |
| `make open` | 產生並開啟 Xcode |
| `make doctor` | 環境自我檢查 |
| `make reference` | 跑 Python 參考實作，重新產生黃金向量 |

改 `.swift` 內容不用 `make generate`，Xcode 直接 ⌘R。

Docker 在這台機器上是可用的，`make docker-test` 會把同一批測試在 Linux 上跑一次。
CI 另外還有 `.github/workflows/swift.yml`。

**`make docker-test` 會偶發卡死**，症狀見 `Packages/TurntableCore/Makefile` 的註解
（Docker Desktop for Mac 的 VirtioFS）。一個晚上卡了三次，殘骸會留著累積
（`--rm` 只在正常結束時自刪）。看到 `docker ps` 有 `swift test` 的 container 掛著
就是卡了，`docker rm -f <名字>` 清掉。**不要用 `| grep | tail` 包起來跑** ——
管線會把輸出整批緩衝，看不出是卡住還是在跑。

## 已實測的數據 —— 不要重新推導

| 項目 | 值 | 來源 |
|---|---|---|
| iPhone 15 Pro Max 陀螺儀校準倍率 k | **0.99915** | 碼錶三次，其中兩次與 App 同步 |
| 該陀螺儀比例因子誤差 | **+0.085%**（等於沒有誤差） | 同上 |
| TD 235 EV 真實轉速（33⅓ 設定） | **31.945 RPM（−4.16%）** | 碼錶 101 圈/189.6 s、100 圈/187.91 s |
| TD 235 EV 需要提高 | **+4.34%** | 同上 |
| 官方 W&F 規格 | 0.07% | Thorens 手冊 |
| 實測取樣率 | 100.1 Hz | 實機 |
| 磁北參考 | 可用 | 實機 |
| **融合路徑自動校準** | **失敗，k 估成 0.99994 / 0.99999** | 實機兩次，68.1 s 與 67.7 s |
| **原始磁力計路徑** | **手機偏心擺時失敗（見坑 13、14、15）；置中後 k 準到 0.06%（坑 32）** | 實機多次 |
| **皮帶循環頻率** | **0.4864 Hz（轉盤的 0.9107 倍）**，n=1..6 諧波全在 | 201 s，20260901-155337 |
| **馬達特徵頻率** | **18.850 Hz（轉盤的 35.29 倍）**，跨三場次差 0.02% | 同上 |
| 每圈一次的磁場失真 | 峰谷差 **29.9 µT**，大過訊號振幅 26.0 µT | 匯出資料按圈內相位分箱 |
| 手機附近的垂直磁場 | 約 **470 µT**（MagSafe 磁鐵環量級） | 未校準磁力計原始讀數 |
| 量測現場的本地磁場 | 垂直約 **+97 µT**（地磁的 2 倍以上） | 原始磁場 (−6.6, −38.1, +71.2) µT |

三次碼錶（0.99953 / 0.99877 / 0.99931）落在 0.08% 範圍內，其中兩次與 App 同步量測
（同一段轉動，排除唱盤本身漂移的干擾）。單次校準精度 ±0.16%。

**App 的重現性比碼錶好**：兩次同步量測的 App 未修正讀數只差 0.023%，
碼錶則差 0.100%。人為按錶誤差已經是瓶頸，碼錶沒辦法再驗證 App 到更細。

## 踩過的坑 —— 這一節是本文件最重要的部分

1. **模擬器沒有陀螺儀。** 任何感測器相關的東西都必須實機測試。`Core/` 切成不依賴 iOS
   框架就是為了讓演算法能在 Mac 上跑測試，不用上機。

2. **移動平均只能用在顯示路徑，絕對不能用在抖晃率計算。** N 個樣本的移動平均在
   fs/N 有零點；100 Hz 下 N=25 的零點正好落在加權曲線的 4 Hz 峰值上，會把要量的東西
   整個挖掉。「最大偏差 %」一定要連同它的平滑視窗一起回報，否則是無法比較的數字。

3. **`PhaseIntegrator` 的錨點要推進「剛好一整圈」，不能設成偵測到的實際位置。**
   偵測必然落在跨圈後的第一個取樣，超調量若不丟掉會逐圈累積：45 轉 100 Hz 每圈
   133.33 個取樣，偵測落在第 134 個，每圈多 1.8°，60 秒 44 圈累積 79°。

4. **不能假設 yaw 是遞增的。** 唱盤從上方看是順時針轉，裝置姿態的 yaw 遞減。
   舊版無條件 `+= 2π`，錨點朝反方向跑掉，之後每個取樣都滿足「走了一圈」——
   真機上量到 4921 個樣本卻報 4730 圈。**合成訊號有 `reversedYaw` 參數，
   凡是會累積或比較 yaw 的東西都要測正反兩個方向。**

5. **時間戳一定要用 `CMLogItem.timestamp`**，用相鄰樣本的實際差值積分。
   CoreMotion 的派送有抖動，假設等間隔會累積角度誤差。

6. **不要只讀陀螺儀 z 軸。** 要把三軸角速度投影到重力方向（`SpinProjector`）。
   傾斜 5° 的 cos 誤差就是 0.38%，超過目標精度。實機上 iPhone 靠在鏡頭凸起會傾 0.8°。

7. **黃金值來自 `Reference/` 的 Python 實作，不是 Swift 自己的輸出。** 改演算法的流程是：
   先在 Python 改 → `make reference` 確認數學 → 再同步到 Swift → `make test`。

8. **搬動資料夾後要 `rm -rf .build`。** 預編譯模組快取（.pcm）把絕對路徑烙在檔案裡，
   路徑一變就整批拒收，錯誤訊息是 `missing required module 'SwiftShims'`。

9. **XcodeGen：Team ID 放在 `Config/Local.xcconfig`。** 在 Xcode GUI 裡選的 Team 會被
   `make generate` 洗掉。

10. **免費 Apple ID 的簽章 7 天過期**，App 打不開時接上線 ⌘R 重裝即可。

11. **`CMDeviceMotion.attitude.yaw` 不能拿來校準陀螺儀 —— 它是融合結果。**
    盤面以 190 °/s 連續轉動時 CoreMotion 會把磁力計降權到幾乎沒有貢獻，yaw 退化成
    陀螺儀積分本身。實測 68.1 秒 36 圈：陀螺儀總轉角 12987°、磁北總轉角 12986°，
    只差 1°，而真實轉角是 13242°（碼錶推算）—— **兩條路徑一起少了 255°**，
    於是不管陀螺儀準不準，它都會吐出 k ≈ 1。那次估的 0.99994 跟後來碼錶量到的
    0.99915 只差 0.08% —— **但那是矇對的，不是量對的**，換一支有真實比例因子誤差的
    陀螺儀它一樣會回報 1。
    **兩條路徑吻合到雜訊以下時，「陀螺儀很準」和「yaw 就是陀螺儀積分」無法區分；
    資料本身不含這個資訊。** 判斷可信度用 `ScaleCalibrator.confidence`，
    判準是分歧量而不是圈數（舊 UI 用 `revolutions >= 30`，在 36 圈時對著一個
    「無法區分對錯」的數字說「可以參考了」）。獨立的路徑是 `MagneticRevolutionCounter`：
    直接數 `magneticField.field` 的原始向量轉了幾圈，完全不碰融合器。

12. **量測記錄一定要能匯出原始逐樣本資料。** 磁場的問題光看摘要數字診斷不出來 ——
    實測遇到「圓擬合的半徑/偏移」與「min/max 推出的半徑/偏移」互相矛盾
    （24.9/11.3 vs 16.3/21.7），只有拿到逐樣本向量分段擬合才看得出圓心有沒有漂移。
    App 停止量測後會寫一份 JSON（`MeasurementExport`），同時記錄
    `CMDeviceMotion.magneticField`（已校準）與 `CMMagnetometerData`（未校準）兩種來源，
    用 `tools/analyze_export.py` 分析。

13. **原始磁力計路徑會被「跟著手機一起轉的磁鐵」整條打死。**
    地磁在裝置座標系裡畫一個圓：半徑是地磁水平分量，圓心偏移是本地磁場。
    **圓要包住原點才繞得起來**，也就是需要「地磁水平分量 > 本地磁場」。
    實測那次盤面轉 35 圈（12950°），地磁總轉角只累積 576° —— 圓心被推出半徑之外，
    角度只能來回擺盪。診斷指標是水平分量的 min/max：
    `(max+min)/2` 與 `(max−min)/2` 分別是兩者，誰是誰由有沒有繞圈決定
    （`MagneticRevolutionCounter.horizontalRange`）。
    台灣的地磁水平分量約 37 µT，本地磁場只要超過這個數這條路就不成立。
    **Apple 原廠矽膠殼就有 MagSafe 磁鐵環** —— 實測拿掉手機殼後本地磁場從
    81 µT 掉到 17.6 µT，是最大宗來源。

14. **投影取水平分量時，垂直分量一定要用「當下這一筆」的重力扣掉。**
    只用固定基底做內積會讓強垂直磁場隨盤面章動洩漏進來：實測手機附近有 470 µT
    垂直場，盤面軸相對鉛直傾斜 1.8°，洩漏量 470 × sin(1.8°) ≈ 15 µT，
    而水平訊號振幅只有 25 µT，整個被蓋掉。
    **實測：固定基底解捲只有 1832°，先扣掉瞬時垂直分量則是 12982°（陀螺儀 12928°）。**
    基底本身仍固定在第一筆樣本（它只決定角度原點），逐樣本重算反而會把章動灌進角度。

15. **磁力計自動校準最後還是失敗了，主因是每圈一次的空間磁場失真。**
    修好洩漏之後地磁確實繞得起來，但殘差按圈內相位分箱顯示峰谷差 29.9 µT，
    比圓半徑 26.0 µT 還大；軌跡有一段幾乎擦過圓心，角度在那裡亂跳、整圈被吃掉。
    切在整數圈上得到 k = 1.00573。**用修正後的碼錶真值 0.99915 重新對照，
    磁力計其實只差 0.5%，比原本以為的準得多**（當時拿錯誤的 1.01837 當基準，
    誤判它「掉了半圈」）。但它對擺放位置極度敏感，換個位置就完全崩掉。
    來源是**房間裡靜止的磁源**加上磁力計離轉軸有距離 —— 跟著盤面轉的東西在裝置
    座標系裡是靜止的、扣圓心就解決，只有靜止磁源的空間梯度會造成這種失真。
    **結論：M3 改走碼錶，磁力計降為診斷工具。**

16. **記錄下來的實測值也可能是錯的；獨立證據跟它衝突時，要查的是記錄值。**
    本文件曾經在「不要重新推導」的表格裡記著 k = 1.01837、唱盤 32.4079 RPM，
    來源是一次碼錶 100 圈 = 185.14 s。後來兩次**與 App 同步**的碼錶量測
    （101 圈/189.6 s、100 圈/187.91 s）給出 k = 0.99953 與 0.99877，
    彼此差 0.076%，而與舊值差 **1.92%**。
    佐證：App 五次未修正讀數全距只有 0.63%，唱盤不可能真的變了 1.43%；
    修好垂直洩漏後的磁力計也獨立指向 k ≈ 1.004。舊值那 2.65 秒的偏差約等於 1.4 圈，
    最可能是數圈失誤。
    **這個錯誤造成的實際損害：** 分析時本來已經從 k≈1 正確推論出「陀螺儀是準的」，
    卻因為本文件標著「不要重新推導」而把正確結論推翻掉，繞了一大圈。
    記錄值與獨立證據衝突時，正確做法是把矛盾攤開，不是讓記錄值自動獲勝。

17. **圓心擬合可以救回「繞不起來」，但前提是圓心在量測過程中固定。**
    圓心是裝置座標系裡的固定向量，盤面轉過幾圈之後資料已經把整個圓掃了很多遍，
    用 Kåsa 代數擬合一次解出來扣掉即可（`MagneticRevolutionCounter.refined()`）。
    合成資料驗證過能還原到 0.02%。
    **但真機上失敗了**：擬合殘差 4.71 µT／半徑 24.9 µT = 19%，而且垂直分量算出來
    只有 −1.8 µT（台灣應該是 26 µT 向下），兩次量測差很多。**資料不是一個穩定的圓。**
    合理的嫌疑是 `CMCalibratedMagneticField` 的偏置估計器在量測過程中持續改動讀數 ——
    待用匯出的原始資料分段擬合驗證。
    `Refined.isTrustworthy` 的殘差門檻本來設 0.25，讓 19% 的爛擬合過關，已收緊到 0.05。

18. **不要讓污染的資料定義什麼叫「正常」。** `StabilityGate` 的中心點用中位數
    而不是平均值，因為平均值本身就會被加速段拉低。但第一版又從離散度那邊把
    問題放回來了：門檻設成 `max(2% × 中位數, 6 × MAD)`，而一段純加速的 MAD
    本來就很大（中位數 150、MAD 75 → 門檻 450），於是加速段自己把自己判成
    正常，整個閘門失效，測試直接抓到。**加上上限 `10% × 中位數` 才修好。**
    真實唱盤的瞬時偏差不會超過 10%，超過就是污染，不該給它寬容度。

    附帶一個限制：中位數只在污染低於一半時可靠。穩定段不到一半的量測
    （例如錄了 10 秒加速卻只有 2 秒穩定）會回 nil —— 承認量測失敗，
    比給一個錯的區間好。

19. **不要拿峰值/RMS 比當二分判準。** 理論值是單頻 1.41、高斯隨機 1.96，
    但實測同一台唱盤兩次得到 1.67 與 1.95 —— 譜峰內容一模一樣，比值本身的隨機
    起伏就跨過了中點，判讀文字因此在「單頻主導」與「隨機雜訊」之間翻面。
    改用 `MeasurementAnalysis.dominantPeakShare`（最強譜峰佔全部譜峰功率的比例），
    那是直接證據，同樣的頻譜會給出同樣的結論。

20. **極座標熱圖的角度不能跨次量測比較。** 0° 是「按下開始那一瞬間」盤面的位置，
    而那是隨機的 —— 兩次量測的峰值角度 152.5° 與 22.5° 看起來差很多，但偏心並沒有
    移動。要能比較就得有物理參考點：盤面貼記號，每次等記號轉到同一位置才按開始。
    這一點要寫在畫面上，否則使用者會做無效的比較。

21. **反旋轉盤面的文字方向：靠「規定擺法 + 正確的角度零點」解決，轉向鍵只是備援。**
    內容鎖在「顯示角度為 0 的那一刻手機的物理方向」。所以只要 (a) 規定固定擺法
    （`PlacementDiagram`：右側長邊中點貼轉軸、機身在左半邊、下緣朝使用者），
    且 (b) 零點落在「手機還照著指示擺著」的時刻，文字就會正對使用者。

    **(b) 一開始是壞的。** `displayAngleDegrees` 讀累積器的角度，而自動模式在
    「等待轉穩 → 開始記錄」時會 `reset()` 累積器 —— 零點因此變成程式決定的隨機時刻。
    修法是把顯示角度獨立成 `displayAngleTotal`，**只在使用者按下開始時歸零**。
    SOP 也跟著改：**轉盤先停著擺好手機 → 按開始 → 才啟動轉盤。**

    轉向鍵留著當備援。控制要**固定在螢幕下緣、不跟著內容轉**、按鈕要夠大 ——
    手機在轉的時候只有位置固定又夠大的東西按得到。這也表示「整片畫面都是停止鍵」
    要縮成「上半部是停止鍵」，否則跟轉向鍵打架。

22. **對 CLAUDE.md 這種長文件做批次字串替換，一定要逐條 assert。**
    今晚有四次替換靜默失敗：整批共用一個 `assert s != o`，只要其中一條命中就通過，
    對錯目標的那幾條就這樣沒進去 —— 上面第 19、20、21 條都是事後才發現漏掉的。
    坑的編號重編過之後，寫死「19. **圓心擬合」這種目標特別容易失效。
    要嘛每條替換各自 assert，要嘛用結構化的定位（例如插在某個標題之前）。

23. **每一條失敗路徑都要有畫面。** 分析回 nil 時（找不到穩定區間 —— 使用者
    根本沒把轉盤打開就按停止，這很常見）原本沒有對應的 UI 分支，畫面就永遠停在
    「分析中…」。加上 `analysisFailureReason` 並把原因講清楚（「整段量測都沒有
    穩定的轉速，轉盤有轉起來嗎？」）。**「載入中」是最容易變成死路的狀態。**

24. **不要無條件訂閱計時器。** 凍結倒數用的 `Timer.publish(every: 0.5)` 原本
    是無條件掛在主畫面上，等於 App 開著就每 0.5 秒重繪一次整個畫面。改成只在
    `freezeUntil != nil` 時才訂閱（用 `Empty` publisher 切換，需要 `import Combine`）。

25. **量測長度會影響譜峰振幅的準確度，不只是解析度。** 實測合成訊號（真值 0.42%）：
    1 分鐘量到 0.387%（低估 8%）、3 分鐘 0.420%、10 分鐘 0.418%。原因是解析度不夠細時
    峰值落在兩個頻率格之間（扇形損失）。**想要準確的振幅要量 3 分鐘以上。**

    順帶記錄效能實測（純 Swift FFT，不需要 vDSP）：1 分鐘 0.13 s、3 分鐘 0.50 s、
    10 分鐘 1.25 s。規格提到的 vDSP 加速目前沒有必要。

26. **目標使用者偏年長，字級與觸控目標要整體放大。** 玩黑膠的人年紀普遍偏大，
    預設的 `.caption`／`.caption2` 對他們太小。做法是把所有語意字級**一次性提升
    一級**（caption2→caption→footnote→subheadline→body），用單次 `re.sub` 加
    callback 完成 —— 分多次替換會把同一處連續升兩級。共 94 處。

    **放大之後有兩個地方一定要重驗：**（a）反旋轉盤面的內接圓約束，因為字變大
    等於內容變高，而高度是最吃緊的方向；（b）列表裡的日期時間會換行
    （`.abbreviated` 變成兩行），要換成更緊湊的格式。

27. **有效傳動比對皮帶厚度極度敏感，不要拿它做二分判定。**
    比值是 `(D+t)/(d+t)`，皮帶輪 8.5 mm 配厚度 0.5 mm 時，光是厚度就讓比值
    差 5.6%。原本用「差 5% 以內就算符合」當門檻，使用者厚度量差一點，
    「這是馬達」的判讀就整個消失。
    改成**永遠把兩個數字並排講出來**（「這台盤的傳動比是 33.4×，量到 35.3×，
    差 +5.6%」）—— 那個資訊本身就有用，它同時可能是「尺寸填錯」或「這根不是馬達」。

28. **手機偏心放在盤上會拖慢轉速、放大抖動 —— 一定要配平。**
    這是實測發現的，而且推翻了先前一次錯誤的判斷。

    實驗：裸機 234 g／對面放等重馬克杯（共 468 g）／手機上疊配重 386 g，三種配置。

| | 裸機 | 配平 | 疊配重 | 唱片鎮置中 | **同左，換手機** |
    |---|---|---|---|---|---|
    | 手機 | iPhone 221 g | iPhone | iPhone | iPhone 221 g | **XZP 191 g** |
    | 轉速（未校準）| 31.9421 | 32.0421 | 31.8098 | 32.0586 | **32.111** |
    | 每圈一次 | 0.4554% | 0.2854% | 0.5487% | 0.2724% | **0.138%** |
    | W&F | 0.1065% | 0.0837% | 0.1251% | 0.0744% | **0.060%** |
    | 時長 | 196.9 s | 48 s | — | 201 s | **194 s** |

    「唱片鎮置中」（2026-09-01，201 s）給出 0.2724%，跟配平的 0.2854% 很接近，
    當時據此寫下「剩下的約 0.27% 是唱盤自己的」。**那個結論後來被推翻了。**

    ⚠ **2026-09-02，Android 版用同樣的擺法、換一支手機（Sony XZP 191 g，
    iPhone 是 221 g）量 194 秒，每圈一次只有 0.138% —— 少了一半。**

    同一次量測裡，**傳動鏈的成分幾乎完全一致**：馬達 18.856 vs 18.8507 Hz
    （差 0.03%）、振幅 0.051% vs 0.0513%（差 0.6%）；皮帶與 4.5× 都在 ±8% 內。
    所以唱盤本身的狀態沒變，變的只有手機。

    **先前那個「收斂」不是證據 —— 兩次都用同一支 iPhone。** 配平與置中是兩種
    不同的機制沒錯，但殘餘的不平衡有一個共同來源：手機的質心不在幾何中心
    （電池、鏡頭模組），肉眼「置中」留下的偏移是實體的。換一支手機，那個偏移就變了。

    **而且 1× 成分是向量相加，不是純量。** 唱盤自己的偏心與手機造成的不平衡有相對
    相位，兩者可能部分抵消 —— 所以 0.138% 連「唱盤的上界」都不算，它可能比唱盤
    自己的值還低。**兩個量值分不開兩個向量。**

    ✅ **2026-09-02 做了「手機轉 180°」的實驗，把兩者分開到一個區間了。**
    同一個唱片鎮、只把手機原地轉 180°，其他都不動，各量 3 分鐘：

    | | A（182 s）| B（194 s，轉 180°）| 差 |
    |---|---|---|---|
    | **每圈一次** | **0.2267%** | **0.1054%** | **−54%** |
    | 轉速 | 32.122 | 32.124 | 0.01% |
    | 皮帶 0.487 Hz | 0.0685% | 0.0683% | 0.29% |
    | 馬達 18.86 Hz | 0.0481% | 0.0488% | 1.46% |

    **對照組完全沒動**（轉速差 0.01%、皮帶 0.29%、馬達 1.46%），只有 1× 少了一半 ——
    正是「手機的貢獻反相、唱盤的不變」該有的樣子。

    1× 是向量相加，A = |D+P|、B = |D−P|，平行四邊形定律給：

        |D|² + |P|² = (A²+B²)/2 = 0.031251
        D·P         = (A²−B²)/4 = 0.010071
        → 兩個量值必為 0.0606% 與 0.1661%

    **所以唱盤自身的偏心落在 [0.061%, 0.166%]，另一個值是手機的貢獻。**
    先前記著的 0.27% 確定是高估。

    一致性檢查：同樣擺法在 02:03 那次量到 0.138%，落在向量和的可能範圍
    [0.105%, 0.227%] 內；iPhone 那次的 0.2724% 也落在裡面。

    ✅ **iPhone 也做了同一組實驗（2026-09-02 19:12 / 19:15，各 3 分鐘），
    兩組區間的交集把答案縮小了一半。**

    | | 1× 的兩次 | 解出的區間 |
    |---|---|---|
    | XZP 191 g | 0.2267% → 0.1054% | [0.061%, 0.166%] |
    | iPhone 221 g | 0.3106% → 0.0806% | [0.115%, 0.196%] |
    | **交集** | | **[0.115%, 0.166%]** |

    **所以這台盤自身的偏心是 0.115%–0.166%。** 先前記著的 0.27% 確定是高估。

    **一個沒有被套進模型、卻自己對上的檢查**：反推兩支手機各自的不平衡，
    XZP 是 0.061–0.134%、iPhone 是 0.155–0.196% —— **重 30 g 的 iPhone 明顯比較大。**
    這個關係完全沒有拿來擬合，兩組實驗各自獨立解出來就對上了。
    先前三次單獨量測（0.2724%、0.138%）也都落在模型允許的範圍內。

    **副產物：2× 不是獨立的「盤面橢圓」。** iPhone 兩次的 2× 是
    0.0924% → 0.0375%（−59%）、3× 是 0.0405% → 0.0215%（−47%）。
    **純粹的 180° 旋轉不該改變 2×**（相位移 2×180° = 360°，等於沒動），
    它卻跟著 1× 一起掉 —— 代表它是跟 1× 耦合的諧波，不是獨立的盤面橢圓。
    非正弦的每圈一次擾動本來就會生出 2×、3×。
    **1× 大的時候，2× 的「盤面橢圓」判讀不可信**，這一點要寫進 app 的文案。

    置中仍然是推薦的擺法：省事、不必找等重的東西、傾角更小（0.55° vs 0.8°），
    而且實測給出這台盤有史以來最低的 W&F（0.060%，低於原廠規格 0.07%）。

    **加了 234 g 卻變快**，加在偏心處反而變慢 —— 主導的不是載重，是不平衡。
    偏心質量在軸承產生側向負載，摩擦拖慢轉速。兩個獨立的量印證：配平後馬達
    輕鬆了 0.195%、傳動比降 0.118%（皮帶少打滑）。

    **後果一：先前用「手機上疊配重」算的載重斜率是錯的。** 兩點外插假設「只有質量
    在變」，那個設計違反了前提，量到的是不平衡不是載重。`LoadCompensator` 用在
    這種場合會給出錯的補償量。

    **後果二：量到的「偏心」有一大部分是手機造成的，不是唱盤的。** 配平後掉三成，
    換一支輕 30 g 的手機再掉一半 —— 而那還沒觸底。

    **修正量測方法優於事後補償** —— 所以解法是把配平寫進 SOP 與擺放圖，
    不是加一個補償係數。

29. **在地化的字串抽取有四個各自獨立的失效方式，每一個都是靜默的。**
    App 已支援繁中／英／日／德四語，字串放在 `App/Localizable.xcstrings`（String Catalog）。

    (a) **自訂 View 的文字參數宣告成 `String` 就不會被抽取。** `DiagnosticRow(_ label: String, …)`
    一口氣讓五十幾條標籤消失 —— 編譯得過、畫面正常，只是永遠不會被翻譯。
    改成 `LocalizedStringKey` 後呼叫端的字面值自動進 catalog，零成本。
    `optionalNumber`／`picker`／`stepperRow`／`safetyRow`／`step`／`line` 全部同一個問題。
    附帶：`Text(.init(someString))` 也是假在地化 —— 它在執行期才組 key。

    (b) **`String(localized:)` 的參數必須是字面值，三元運算式抽不出來。**
    `String(localized: cond ? "甲" : "乙")` 編得過但兩條都不進 catalog；
    要寫成 `cond ? String(localized: "甲") : String(localized: "乙")`。
    **但 `Text(cond ? "甲" : "乙")` 是可以的** —— 那裡每個分支各自轉成 `LocalizedStringKey`。
    兩者長得像，行為不同。

    (c) **語序會變的字串一定要用位置參數。** `String(format:)` 按出現順序填，
    而日文／德文常常要換順序：「相比%@了 %.3f 個百分點」在日文是
    「%2$.3f ポイント%1$@ました」。不用 `%n$` 就會把數字填到動詞的位置。
    驗證要自己寫 printf 掃描器，**正規表示式會誤判** —— `%%` 後面接空白再接 `d`
    （例如 "%% davon"）會被當成 `% d` 轉換符。

    (d) **`xcodebuild` CLI 不會把字串合併進 `.xcstrings`**（那步只在 Xcode IDE 跑）。
    但編譯器有產 `.stringsdata`，是 JSON，`tables.Localizable` 底下一個
    `{comment, key, location}` 的陣列 —— 掃 DerivedData 收集起來就能自己組 catalog。
    注意 DerivedData 可能有多個 `TurntableRPM-*` 目錄，挑最新的那個，
    否則會混進舊的鍵。

    另外兩件設定：**XcodeGen 的 `developmentRegion` 預設是 `en`**，只設
    `DEVELOPMENT_LANGUAGE` 建置設定沒有用 —— 要用 `options.developmentLanguage: zh-Hant`，
    否則不會產生 `zh-Hant.lproj`，中文裝置反而拿到英文。`knownRegions` 則會自動
    從 catalog 讀出來，不用手動維護。

30. **翻完要實際跑一遍，德文會撐爆版面。** 德文平均比中文長一倍以上
    （「Durchmesser des Motorpulleys」對「馬達皮帶輪直徑」）。實測四語系都跑過模擬器：
    導覽頁在 ScrollView 裡所以安全，設定檔表單的長標籤會自動換兩行、數值與步進器仍放得下。
    **唯一真的要縮短的是反旋轉盤面裡的字** —— 那裡有內接圓約束（坑 26），
    「等待轉速穩定」的英文直譯 "Waiting for a steady speed" 太寬，縮成 "Waiting for speed"。
    盤面裡的字在每個語言都要當成「儀表板讀數」來寫，不是句子。

31. **README 截圖的示範資料是臨時 patch，不進版控 —— 重拍前要先重建。**
    模擬器沒有陀螺儀，分析頁與歷史頁在模擬器上永遠是空的，所以截圖需要一個
    `App/DemoData.swift` 把合成分析（`SyntheticSignal` → `MeasurementAnalysis.analyze`）
    塞進 `MotionEngine.init()`，並在 App 進入點 seed 一份 SwiftData
    （`TurntableProfile` + 六筆 `MeasurementRecord`），用 `-demoData` 啟動引數開關。
    拍完 `rm App/DemoData.swift` 並 `git checkout` 那兩個掛勾檔。
    這份 patch 第一次做完就丟掉了，第二次重拍等於從頭推一次，所以把做法記在這裡。

    截圖本身：`xcrun simctl launch <udid> <bundleid> -demoData -AppleLanguages "(en)"`，
    再用 `xcrun simctl io <udid> screenshot`。**每個語言各拍一組**，放在
    `docs/screenshots/<語言>/`。

    **GitHub markdown 表格會依標題文字長度分配欄寬**，圖跟著欄寬縮放 ——
    三張同尺寸的圖在表格裡看起來會一大一小。要等寬就別用表格，改用
    `<p align="center">` 加 `<img width="270">`。

    順帶：截圖不要存原始的 1206×2622，README 顯示寬度只有 270 左右，
    `sips -Z 620` 之後六張總共 384 KB。

32. **把手機放到轉軸正中央，磁力計那條路就活了一半 —— 但仍然不該採信。**
    坑 15 判定失敗的主因是「房間裡靜止磁源的空間梯度 × 磁力計離轉軸的距離」。
    唱片鎮把手機壓在轉軸上，那個距離幾乎沒了，結果（201 s、107 圈）：

    - 圓心在 10 個分段裡都穩定，散佈只有 **0.72 µT**（坑 17 那次圓心會漂、
      分段擬合互相矛盾）
    - 扣掉圓心後：已校準磁場 k = 0.99972、原始磁力計 k = 0.99976 ——
      **兩條獨立路徑彼此差 0.004%，跟碼錶真值 0.99915 差 0.06%**

    這是磁力計第一次給出跟真值一致到碼錶自身精度（單次 ±0.16%）以內的答案。
    **但這不足以復活 M3**：擬合殘差仍有半徑的 31.6%，`refinedTrustworthy` 正確地
    判 False。失真還在，只是 107 圈平均掉了 —— 換個房間、換個位置就未必。
    碼錶仍然是唯一可信的校準路徑。**這條記在這裡是因為它解釋了坑 15 的機制，
    不是因為它推翻了結論。**

33. **匯出的摘要裡，哪些欄位已經套過校準要分清楚，下游工具才不會重複套。**
    `tools/analyze_export.py` 原本把 `meanRPM` 再乘一次 `stopwatchReferenceK`，
    但 App 寫進 `meanRPM` 的已經是套過 `appliedFactor` 的讀數，`rawMeanRPM` 才是未修正的。
    **沒存校準時 `meanRPM == rawMeanRPM`，剛好無害** —— 所以這個 bug 一路潛伏到
    使用者真的存了校準才發作，誤差 0.085%，正好落在目標精度上。
    順帶把那個工具的預設 k 從 1.01837（坑 16 那個作廢的碼錶值）改成 0.99915。

34. **Android 的取樣率是兩層偏移疊出來的，而且必須拿牆鐘去驗時間戳誠不誠實。**
    Sony XZ Premium（G8142、Android 9、STMicroelectronics **LSM6DSM** 陀螺儀）實測：

    | 要求 | 實際 | 間隔中位數 | σ | 抖動比 | 長空隙 | 最糟空隙 | 時間戳÷牆鐘 |
    |---|---|---|---|---|---|---|---|
    | 50 Hz | 53.96 Hz | 18.524 ms | 0.014 ms | 0.075% | 0 | 1.01× | 0.99995 |
    | 100 Hz | **107.92 Hz** | 9.277 ms | 0.015 ms | 0.160% | 0 | 1.00× | 0.99995 |
    | 200 Hz | 215.74 Hz | 4.639 ms | 0.076 ms | **1.641%** | **4** | **1.56×** | 1.00051 |
    | iPhone 15 Pro Max @100 | 100.13 Hz | 9.990 ms | 0.005 ms | 0.05% | 0 | 1.00× | — |

    **那個 +7.9% 是兩層疊出來的，不是雜訊。** LSM6DSM 的 ODR 階梯是
    12.5/26/52/104/208 Hz —— **不是** 50/100/200。HAL 先進位到階梯上，
    再乘一個固定的振盪器偏差：

        53.96 ÷ 52 = 1.03769      107.92 ÷ 104 = 1.03769      215.74 ÷ 208 = 1.03721

    前兩個到小數第五位完全相同，這不是巧合。

    **關鍵是要分辨「感測器真的比較快」還是「時間戳的時鐘比較快」。**
    後者會讓所有頻域結果偏 7.9%，譜峰的「轉盤幾倍」判讀全錯 —— 而那是這個 app
    最有價值的功能。**平均轉速兩種情況都看不出差別**（ω 是物理量，與取樣時鐘無關），
    所以光看轉速查不出來。拿 `SystemClock.elapsedRealtimeNanos()` 對照就分得開：
    實測比值 0.99995，**時間戳誠實**。

    這同時驗證了「一律用 `SensorEvent.timestamp`、絕不假設固定速率」這個設計 ——
    要是當初寫死 100 Hz，這台機器的每一個頻域結果都會偏 7.9%。

    **200 Hz 明顯變差**：抖動比從 0.160% 跳到 1.641%，出現 4 次長空隙、最糟空隙 1.56×。
    而這個 app 根本不需要那麼快（50 Hz 以上只佔加權能量 0.72%），所以固定用 100 Hz 檔，
    也順便完全避開 Android 12 的 `HIGH_SAMPLING_RATE_SENSORS` 權限。

    附帶一個觀察：σ 在 50 與 100 Hz 都是 0.014–0.015 ms，**絕對值固定**，
    抖動比上升純粹是因為間隔變短。到 200 Hz 才真的撐不住。

35. **狀態守衛不能用「發布給 UI 的欄位」—— 那是跨執行緒共用的。**
    `SensorEngine.start()/stop()` 原本拿 `_state.value.running` 當守衛，
    而那個欄位會被感測器執行緒的 `publish()` 覆寫。競態一發生，UI 以為已停止、
    監聽器其實還在，再按一次開始就重複 `registerListener` 而不解除。

    症狀：畫面顯示「開始量測」（停止狀態），`adb shell dumpsys sensorservice` 卻顯示
    `LSM6DSM Gyroscope connections=2`，連線紀錄裡那次 `+` 沒有對應的 `−`。
    漏掉的監聽器會一直吃電，兩份事件流還會互相干擾取樣統計。

    修法是獨立一個 `@Volatile active` 旗標當守衛，並在 `start()` 裡先做一次
    防禦性 `unregisterListener()`。

    **這個 bug 是用 adb 自動化反覆量測時發現的，不是讀程式碼看出來的。**
    `dumpsys sensorservice` 的連線紀錄是 Android 上少數能直接看到「系統認為你註冊了什麼」
    的地方，值得記住。

36. **靜止不動的手機會「通過」穩定閘門 —— 因為它確實很穩。**
    這條是 Android 版第一次跑實機 UI 就撞到的，而**同一個 bug 一直存在於 iOS 版**。

    手機放桌上不動、按開始再按停止，分析沒有失敗，反而吐出
    **「抖晃率 24.593% WRMS、DIN 2σ 54.912%」**。

    原因：MEMS 陀螺儀靜止時輸出的**不是雜訊，是幾乎固定的偏置**（實測 0.006 RPM）。
    `StabilityGate` 檢查的是「穩不穩」，那段資料非常穩，於是整段放行；
    接著 `DeviationSeries` 除以一個趨近零的平均值，偏差百分比就爆掉。

    **閘門沒有錯，它只是被問錯了問題。** 「這段資料穩不穩」和「唱盤有沒有在轉」
    是兩件事，前者答對了不代表後者成立。修法是在 `MeasurementAnalysis.analyze`
    加一個最低轉速下限（`minimumRPM = 5.0`）—— 遠低於最慢的標稱轉速 16⅔，
    不會誤擋任何真實唱盤。兩邊都補了回歸測試（Swift 99 個、Kotlin 64 個）。

    附帶：失敗訊息要分三種。閘門找不到區間 → 「轉速不穩」；找得到但低於下限 →
    **「轉盤沒有轉起來」**；其餘 → 「資料不足」。第二種最容易搞混 ——
    對一個沒把轉盤打開的使用者說「轉速不穩」，他會完全找不到方向。

    **這說明了移植的價值不只是多一個平台。** 換一個環境跑同一套演算法，
    會逼出原本沒人走過的路徑。

37. **不要拿背景負載不同的兩次量測互相比較 —— 而最常見的污染源是自己的工具鏈。**

    症狀：量測畫面的抖動比 0.811%，同一台手機在純診斷畫面是 0.160%。
    我當下的判斷是「`SamplingStats.from()` 內含排序，每 200 ms 在感測器執行緒上
    對全部樣本排一次，卡住了事件處理」，並據此把統計移到單執行緒 executor 上。

    **結果修完更糟（1.047%），假設被自己的資料推翻。**

    做了對照實驗才看出來：同一場次、同樣 26 秒，量測畫面得到 **0.216%**，
    跟診斷畫面的 0.160% 幾乎一樣。差異根本不存在。

    真正的原因是**那兩次量測都緊接在 `adb install` 之後跑**，ART 的 profile 最佳化
    還在背景做（logcat 裡有 `ProfileInstaller`）。我用來自動化量測的工具本身
    就是污染源。

    **教訓有兩層。** 一是比較必須同場次、同條件 —— 這跟坑 16「記錄值與獨立證據
    衝突時要查記錄值」是同一類錯誤，只是這次錯的是我當下的推論而不是舊記錄。
    二是**自動化量測會把自己的成本算進被量的東西裡**，量測前要留時間讓裝置安定。

    那個執行緒的改動留著 —— 在感測器回呼上做 O(n log n) 本來就不對，3 分鐘的量測
    是兩萬筆，遲早會出問題。但它**沒有修好任何已觀察到的症狀**，不能算是修 bug。

38. **對外讀數與校準都要用「切過的」平均值 —— iOS 早就修過，Android 又犯一次。**
    Android 版第一次在真唱盤上跑就露出來：大字讀數 **31.546 RPM**，
    而分析卡片（切掉開頭 1.4 秒的加速）算出來的是 **32.15**，差 **1.9%**。

    兩個數字不一致本來就會讓人困惑，但**真正危險的是校準拿到污染的值** ——
    校準畫面吃的是整段平均，那個 k 會被永久寫進偏好設定，之後每一次讀數都錯，
    而且看不出來。iOS 端的筆記早就寫著「校準也改用切過的平均值」，
    移植時只搬了演算法沒搬這個決定。

    修法是分析完成後把 `meanRPM` / `rawMeanRPM` / 標稱辨識全部換成穩定區間的值，
    並在分析卡片上把「平均轉速（切過）」一起列出來，讓兩個數字的關係看得見。

    **移植時要搬的不只是程式碼，還有那些寫在筆記裡、程式碼上看不出來的決定。**

39. **`_state.value = _state.value.copy(...)` 是讀-改-寫，多執行緒下會靜默吃掉更新。**
    使用者連做兩次量測，畫面完全沒反應 —— 沒有新結果，**也沒有失敗訊息**，
    停在上一次的舊結果上。那是最糟的失敗型態：看起來像什麼都沒發生。

    `dumpsys sensorservice` 的註冊紀錄證明量測確實跑了（兩段 39 s 與 38 s），
    logcat 沒有例外。問題出在 `StateFlow` 有**三個**寫入者：
    主執行緒的 `stop()`、publish 用的 executor、以及分析用的執行緒。
    分析寫完之後，一個排在後面的 publish 任務用它先前讀到的舊值覆蓋回去。

    改用 `MutableStateFlow.update { }`（內部是 compare-and-set 迴圈）就沒事了。

    **順帶修掉一個相關的錯誤**：分析失敗的早退路徑只設 `analysisFailureReason`、
    沒有清掉 `analysis`，所以失敗時畫面會留著**上一次成功的結果**。
    那比沒有結果更糟 —— 使用者會以為那是這次量到的。

    教訓：**「畫面沒反應」不等於「程式沒跑」。** 先看系統層的證據
    （`dumpsys sensorservice` 的註冊紀錄、logcat），再回頭看程式。

40. **沒有歷史記錄的量測 app，做不了任何需要比較兩次的實驗。**
    使用者做「手機轉 180° 再量一次」的實驗（要分離唱盤自身偏心與手機造成的不平衡），
    連做兩次之後才發現 **Android 版根本沒有歷史記錄**，第一次的結果已經沒了。

    iOS 版早就有（`MeasurementRecord` + SwiftData），而且筆記裡寫明「分析一完成就
    自動存檔，不做手動按鈕 —— 使用者不會記得按，而歷史的價值就在調整前後能比較」。
    移植時漏掉了。

    這跟坑 38 是同一類：**移植要搬的不只是程式碼，還有那些決定。**

41. **時序圖不能用「每 N 筆取一筆」壓縮 —— 那是重新取樣，會混疊。**
    12000 筆偏差序列抽成 600 點等於用 5 Hz 重新取樣，3.15 Hz 的抖晃在畫面上
    變成一個根本不存在的低頻波紋，振幅也被低估（±0.69% vs 真值 ±0.71%）。
    畫面在說謊，而數字是對的 —— 這種不一致最難發現。

    正確做法是**每一個像素欄畫該區段的最小到最大值**（波形封包），再把同一段的
    平均疊上去：淡色的帶是快的抖，深色的線是慢的漂，兩者在同一張圖上分得開。
    壓縮只影響畫面，不影響任何數字。

    **iOS 的 `AnalysisView.rollingPoints` 是同一個寫法，還沒修。**

42. **macOS 的檔名不分大小寫 —— 這個 repo 裡 `app/` 就是 `App/`。**
    多語系的產生腳本被傳錯根目錄，在 repo 根建出 `app/src/main/res/...`；
    而那條路徑實際落在 iOS 的 `App/` 裡面。接著一句 `rm -rf app` 想清掉那批垃圾，
    刪掉的是**整個 iOS 原始碼目錄**（22 個 .swift 加 Assets.xcassets）。

    還原靠 `git checkout -- App/`，因為那些檔案都已經 commit 過，一個字都沒丟。
    **這是「刪除前先看目標」的具體版本**：`ls app` 會列出 `App/` 的內容，
    看一眼就知道那不是剛產生的東西。

    腳本現在會先 assert 根目錄底下有 `app/build.gradle.kts`，傳錯就不動手。

43. **Compose 的 `stringResource` 是 @Composable，純函式的文案助手全部要改。**
    Android 版多語系化時，`interpretation(peak)`、`verdict(result)`、
    `changeDescription(points)` 這幾個「回傳一句話」的純函式都拿不到資源，
    只能改成 `@Composable`（或改成直接發 `Text`）。感測器層更麻煩 ——
    `SensorEngine` 根本不在 Compose 裡，錯誤訊息要走 `context.getString()`，
    而且要拿 `applicationContext`：引擎的生命週期比 Activity 長。

    資料類別同理。`TurntableProfile.displayName` 原本在空白時回「未命名唱盤」，
    那句中文寫死在資料層等於把它排除在多語系之外。**預設值要往 UI 層推**，
    模型回空字串，畫面用 `ifBlank { stringResource(...) }` 補。

    另外兩個 Android 特有的點：
    - **含兩個以上參數的字串一定要用 `%1$s` 這種位置參數**，aapt2 會直接擋下
      非位置的多參數字串。而那正好也是日文與德文換語序需要的東西
      （「相比%1$s了 %2$.3f 個百分點」在日文是「%2$.3f ポイント%1$sしました」）。
    - `values/` 是找不到符合語系時的後備，所以那裡放**英文**不是中文。
      Android 13 以上還要 `res/xml/locales_config.xml` 加 `android:localeConfig`，
      否則系統設定裡不會出現「單獨指定這個 app 的語言」。

## 設計原則

- **比例因子校準原本被當成成敗關鍵，實測後證明不是。**
  舊版這一節寫著「實測是 1.8%，沒有校準其他都是白做」—— 那個 1.8% 來自一筆錯誤的
  碼錶量測（見坑 16）。與 App 同步量測三次的結果是 **0.085%**，比校準本身的
  不確定度（±0.11%）還小。陀螺儀的隨機雜訊平均 10 秒後只有 0.0011%，一樣不是問題。
  **這支陀螺儀實務上不需要修正。** 校準功能仍然值得留著 —— 它證明了這件事，
  而且別台裝置未必一樣 —— 但它不再是專案的成敗所在。
  這一條在文件裡錯了好幾個小時沒被發現，本身就是坑 16 的例證。
- **顯示路徑與分析路徑必須分開。** 見坑 2。
- **量的是「盤」，不是「音樂」。** 唱片中心孔偏心造成的 wow 這個方法完全看不到，
  而那在實務上經常是最大宗。這一點要誠實寫進 app 的說明頁。
- **iOS 的 100 Hz 取樣上限不是問題。** 白頻譜假設下 50 Hz 以上只佔加權能量 0.72%，
  WRMS 只低估 0.36%。

## 進度

- **M0 演算法核心** — 完成，74 個測試全過（macOS 與 Linux container 都驗過）。
- **M1 感測器接通** — 完成，實機驗證過（取樣率 100.1 Hz、重力投影、圈數正確）。
- **M2 標稱辨識 / 反旋轉盤面 / 停止凍結** — 大部分完成。標稱辨識、畫面重整、
  **反旋轉盤面**（`SpinningDialView`，規格 §6.2）與**停止凍結**都做好了。
  另加**手動／自動兩種模式**（**預設自動**）：自動模式等轉速穩定才開始記錄、
  盤面停下時自己結束。首次開啟有四頁操作導覽（`OnboardingView`），第二頁的擺法圖同時是文字方向問題的解法。
- **M3 校準** — **完成。碼錶校準是唯一可信的路徑**
  （`StopwatchCalibration` + `CalibrationStore` + `StopwatchCalibrationSheet`）：
  輸入圈數與秒數算出 k，連機型一起存進 UserDefaults，之後所有對外讀數自動套用；
  機型對不上（換機／備份還原）就失效並在畫面上說明；離譜的 k（0.8–1.25 之外）
  會被擋，那幾乎一定是輸入打錯。
  **指南針自動校準兩條路都失敗**（見坑 11、15），降為診斷工具，畫面上明確標示不可採信。
- **M4 滾動圖 / 熱圖 / 頻譜 / 歷史** — 大部分完成。
  `MeasurementAnalysis`（核心，所以能在 Linux CI 上測）把重取樣、偏差序列、
  加權抖晃率、FFT、譜峰判讀、極座標分箱組合成一次分析。`AnalysisView` 呈現：
  抖晃率、**譜峰判讀**、頻譜圖、極座標熱圖、滾動偏差圖。
  **譜峰判讀是這個 App 的核心價值** —— 整數倍代表跟著盤面轉的東西（偏心、變形），
  非整數倍代表傳動鏈上轉速不同的零件。這正是先前用 Python 手動做的診斷。
  `StabilityGate` 會自動切掉開頭的加速、尾端的減速與中途的干擾，並在畫面上
  如實回報切了多少 —— 校準也改用切過的平均值，否則使用者拿含加速的資料
  校準會把 k 永久寫錯。
  **SwiftData 歷史已完成**（`MeasurementRecord` + `HistoryView` + `TrendChart`）：分析一完成就
  自動存檔，不做手動按鈕 —— 使用者不會記得按，而歷史的價值就在「調整前後能比較」，
  漏存一次就斷了。**只存分析後的摘要，不存逐樣本原始資料**（一次 3 分鐘的量測是
  兩萬筆、約 700 KB，存幾十次資料庫就肥了）；要原始資料就用匯出。
- **M5 唱盤設定檔 / 載重補償** — 完成。`TurntableProfile` 存唱盤自己的性質
  （原廠 W&F 規格、傳動鏈尺寸、載重敏感度），分析頁會自動跟規格比對，並把
  「非諧波的某某倍」對上這台盤的傳動比認出馬達。`LoadTestView` 的兩點外插
  **直接從歷史挑兩筆記錄**，不做成特殊量測模式 —— 使用者只要正常量兩次。
  原始資料匯出（`MeasurementExport`）搭配 `tools/analyze_export.py`。

畫面已重排完成。`LiveMeasurementView` 是主畫面（轉速讀數 → 安全提醒 → 碼錶校準
→ 這次量測 → 匯出），兩條失敗的自動校準路徑收進 `AdvancedDiagnosticsView`
（NavigationLink 進去）。`AboutView` 是說明頁，誠實交代「量的是盤不是音樂」、
未校準時偏差% 不能用、以及安全事項。

App 圖示已完成（`tools/make_icon.py` 產生，可重跑）—— 唱片加一道琥珀刻度，
那道刻度就是碼錶校準要你做的事。**檢查圖示要先看 60px 再看 1024**：
頻閃盤與抖晃波兩個概念上更貼切的方案都是在小尺寸糊掉才被淘汰的，
失敗原因記在 `tools/make_icon.py` 的檔頭。

**在地化** — 完成。繁體中文（來源）／英文／日文／德文，306 條字串，
`App/Localizable.xcstrings`。四語系都在模擬器上實際跑過版面。日文與德文由 Claude 翻譯，
還沒有母語者審過。

**Android 版** — 功能上已與 iOS 對齊。核心 92 個 JVM 測試（含
`MagneticRevolutionCounter` / `ScaleCalibrator` / `CalibrationConfidence`，
與 Swift 端同一組情境），app 層另有 17 個。**拿 iOS 匯出的真實錄音交叉驗證過：
兩個實作的結果差 0.002% 以內**（`make android-crosscheck`）。CI 上會跑核心測試、
app 測試與 debug APK 建置。

畫面：量測主畫面、反旋轉盤面、自動／手動模式、碼錶校準、分析卡片與三張圖
（頻譜、極座標熱圖、瞬時偏差）、歷史列表與詳情（含備註）、趨勢圖、唱盤設定檔、
載重測試、說明頁、四頁導覽、取樣特性診斷、進階診斷、原始資料匯出。

**多語系完成**：繁中／英／日／德，353 條字串。`values/` 放英文（找不到符合語系時的
後備），繁中在 `values-zh-rTW/`。**四個 `strings.xml` 不要手改** ——
改 `android/tools/strings_catalog.json` 再跑 `make android-strings`，
那支腳本會強制四個語系的鍵完全一致、含參數的字串位置參數齊全。

還沒做：release 簽章與上架。

**還沒做的上架必要項目**：付費開發者帳號、上架素材（截圖／描述／隱私問卷）。
截圖必須用實機拍 —— 模擬器沒有陀螺儀，畫面永遠是空狀態。

## 延伸閱讀

- `docs/spec.md` — 完整技術規格，含演算法推導、誤差預算、加權曲線、熱圖相位分析、
  校準所需圈數、載重兩點外插法。**改演算法前先讀對應章節。**
- `docs/device-setup.md` — 上機步驟與疑難排解。
- `Packages/TurntableCore/README.md` — 模組對照表與測試涵蓋範圍。
